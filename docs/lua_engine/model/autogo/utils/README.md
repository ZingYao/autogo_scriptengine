# utils 模块

## 模块简介

utils 模块提供了各种实用工具函数，包括日志记录、数据转换、加密解密、编码解码等。

## 方法列表

### utils.logI
记录一条INFO级别的日志

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| label | string | 是 | 日志标签 |
| message | string | 是 | 日志消息（支持多参数，会自动用空格连接） |

**返回值：**

无

**使用示例：**
```lua
-- 调用 utils.logI 方法
utils.logI("TAG", "这是一条日志")
```

---

### utils.logE
记录一条ERROR级别的日志

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| label | string | 是 | 日志标签 |
| message | string | 是 | 日志消息（支持多参数，会自动用空格连接） |

**返回值：**

无

**使用示例：**
```lua
-- 调用 utils.logE 方法
utils.logE("TAG", "这是一条错误日志")
```

---

### utils.toast
显示Toast提示

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| message | string | 是 | 提示消息内容 |
| x | number | 否 | 在界面上显示的 X 坐标（传递-1使用默认坐标） |
| y | number | 否 | 在界面上显示的 Y 坐标（传递-1使用默认坐标） |
| duration | number | 否 | 提示显示的持续时间，单位为毫秒（传递-1使用默认2000毫秒） |

**返回值：**

无

**使用示例：**
```lua
-- 调用 utils.toast 方法
utils.toast("操作成功")

-- 带位置和时长的调用
utils.toast("操作成功", 100, 200, 3000)
```

---

### utils.alert
显示Alert对话框

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| title | string | 是 | 对话框标题 |
| content | string | 是 | 对话框内容 |
| btn1Text | string | 否 | 第一个按钮文本（可选） |
| btn2Text | string | 否 | 第二个按钮文本（可选） |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | number | 用户点击的按钮索引（从0开始） |

**使用示例：**
```lua
-- 调用 utils.alert 方法
local result = utils.alert("提示", "确定要继续吗？", "确定", "取消")
```

---

### utils.shell
执行shell命令并返回输出

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cmd | string | 是 | shell命令字符串 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | string | 命令执行结果 |

**使用示例：**
```lua
-- 调用 utils.shell 方法
local output = utils.shell("ls -l")
```

---

### utils.random
返回指定范围内的随机整数

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| min | number | 是 | 最小值 |
| max | number | 是 | 最大值 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | number | 随机整数 |

**使用示例：**
```lua
-- 调用 utils.random 方法
local num = utils.random(1, 100)
```

---

### utils.sleep
暂停当前线程指定的毫秒数

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| milliseconds | number | 是 | 暂停的毫秒数 |

**返回值：**

无

**使用示例：**
```lua
-- 调用 utils.sleep 方法
utils.sleep(1000)
```

---

### utils.i2s
将整数转换为字符串

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| i | number | 是 | 整数值 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | string | 转换后的字符串 |

**使用示例：**
```lua
-- 调用 utils.i2s 方法
local str = utils.i2s(123)
```

---

### utils.s2i
将字符串转换为整数

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| s | string | 是 | 字符串值 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | number | 转换后的整数 |

**使用示例：**
```lua
-- 调用 utils.s2i 方法
local num = utils.s2i("123")
```

---

### utils.f2s
将浮点数转换为字符串

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| f | number | 是 | 浮点数值 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | string | 转换后的字符串 |

**使用示例：**
```lua
-- 调用 utils.f2s 方法
local str = utils.f2s(3.14)
```

---

### utils.s2f
将字符串转换为浮点数

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| s | string | 是 | 字符串值 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | number | 转换后的浮点数 |

**使用示例：**
```lua
-- 调用 utils.s2f 方法
local num = utils.s2f("3.14")
```

---

### utils.b2s
将布尔值转换为字符串

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| b | boolean | 是 | 布尔值 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | string | 转换后的字符串（"true"或"false"） |

**使用示例：**
```lua
-- 调用 utils.b2s 方法
local str = utils.b2s(true)
```

---

### utils.s2b
将字符串转换为布尔值

**参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| s | string | 是 | 字符串值 |

**返回值：**

| 返回值 | 类型 | 说明 |
|--------|------|------|
| result | boolean | 转换后的布尔值 |

**使用示例：**
```lua
-- 调用 utils.s2b 方法
local bool = utils.s2b("true")
```

---

## 综合使用示例

### 示例1：日志和提示
```lua
utils.logI("TAG", "这是一条日志");
utils.toast("操作成功");
```